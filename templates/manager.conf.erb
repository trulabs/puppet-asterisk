[general]
displaysystemname = no
enabled = <%= @manager_enabled || 'no' %>
webenabled = <%= @manager_webenabled || 'no' %>
port = <%= @manager_port || '5038' %>
bindaddr = <%= @manager_bindaddr || '127.0.0.1' %>
